# Frontend Packages

Planned package boundaries:

```text
services
ui-assets
ui-library
ui-tokens
utils
```

These should be adapted into AIVIS-local packages rather than imported directly
from upstream design-system references.
