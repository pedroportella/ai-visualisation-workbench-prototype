# Docker

Use this folder for local runtime and deployment helpers once the backend/API
spine exists.

Initial Docker work should support reproducible local smoke checks before any
production-style claim is made.

