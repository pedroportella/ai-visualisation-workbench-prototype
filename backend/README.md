# Backend

The first backend stage should create a minimal API spine:

- health endpoint;
- mode/version metadata endpoint;
- deterministic answer fixture endpoint;
- deterministic source inventory fixture endpoint;
- deterministic evidence graph fixture endpoint;
- contract tests.

This should not become a full RAG, GraphRAG or cloud platform implementation
until the Evidence Workbench contract is stable and the capability is verified.

